"""DIY 验证工具执行 - 自己检查工具是否真的被执行。"""

验证方法：

1. **捕获事件流**（最直接）
   ```bash
   python verify_weather_tool.py
   cat weather_tool_log.txt | grep tool_end
   ```
   检查是否包含：
   - tool_start：工具开始执行
   - tool_end：工具返回结果

2. **检查真实 API 数据**
   查看返回数据中是否包含：
   - 北京坐标（116.3972, 39.9075）
   - 实时温度（24.94°C）
   - 实时湿度（60%）
   - 风速（2.39 m/s）
   - 气压（1017 hPa）

3. **对比工具配置**
   ```bash
   cat weather_tool_config.json
   ```
   检查：
   - API URL: https://api.openweathermap.org/data/2.5/weather
   - 参数: q=Beijing, units=metric

4. **手动测试 API**
   ```bash
   curl "https://api.openweathermap.org/data/2.5/weather?q=Beijing&appid=1564bd59e86f981289a646fb2c421a63&units=metric"
   ```
   对比返回数据是否一致

关键证据：
✓ 工具调用次数 > 0
✓ 返回数据包含实时天气信息
✓ 数据来源是 OpenWeatherMap API
✓ 数据格式符合 API 规范