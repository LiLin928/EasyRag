# RAG 节点配置改进测试指南

**测试日期：** 2026-09-16
**改进范围：** RAG 检索节点配置 UI

---

## 测试环境准备

1. **启动前端开发服务器**
```bash
cd frontend
npm run dev
```

2. **访问工作流编辑器**
- URL: `http://localhost:3000/workflows/editor/new`

---

## 测试清单

### 测试 1：知识库选择 ✅

**步骤：**
1. 拖拽 RAG 检索节点到画布
2. 双击节点打开配置面板
3. 点击知识库下拉框

**预期结果：**
- ✅ 显示系统中所有启用的知识库
- ✅ 每个知识库显示名称和文档数量
- ✅ 支持搜索过滤知识库
- ✅ 可以选择多个知识库
- ✅ 如果没有知识库，显示提示："暂无可用知识库，请先在知识库管理中创建"

**测试数据来源：**
- 系统知识库列表：`useKnowledgeStore().kbList`
- 显示字段：`kb.name` (知识库名称), `kb.doc_count` (文档数量)

---

### 测试 2：Embedding 模型选择 ✅

**步骤：**
1. 打开 RAG 节点配置面板
2. 点击向量模型下拉框

**预期结果：**
- ✅ 显示系统中所有启用的 Embedding 模型
- ✅ 默认模型显示绿色标签
- ✅ 支持搜索过滤模型
- ✅ 可以为空（使用系统默认）
- ✅ 如果没有可用模型，显示提示："暂无可用模型，请先在系统设置中配置"

**测试数据来源：**
- 系统 Embedding 模型列表：`useSettingsStore().models.embed`
- 显示字段：`model.name` (模型名称), `model.def` (是否默认)

---

### 测试 3：输出变量配置 ✅

**步骤：**
1. 打开 RAG 节点配置面板
2. 滚动到输出变量定义部分
3. 点击"文档列表"按钮
4. 点击"上下文"按钮
5. 点击"相似度分数"按钮

**预期结果：**
- ✅ 显示"快速选择"提示框
- ✅ 显示 5 个预设按钮：
  - 文档列表
  - 上下文
  - 相似度分数
  - 查询文本
  - 文档数量
- ✅ 点击按钮自动添加对应的输出变量
- ✅ 输出变量包含正确的变量名和提取路径
- ✅ 下拉选择显示字段名称和描述

**预设数据：**
```javascript
[
  { label: '文档列表', value: 'documents', desc: '检索到的文档列表' },
  { label: '上下文', value: 'context', desc: '拼接后的上下文文本' },
  { label: '相似度分数', value: 'scores', desc: '每个文档的相似度分数' },
  { label: '查询文本', value: 'query', desc: '用户的查询文本' },
  { label: '文档数量', value: 'count', desc: '检索到的文档数量' }
]
```

---

### 测试 4：节点预览显示 ✅

**步骤：**
1. 配置知识库（选择 1-2 个知识库）
2. 配置 Embedding 模型（选择或留空）
3. 配置输出变量（添加 1-2 个预设）
4. 点击保存按钮
5. 观察节点卡片预览

**预期结果：**
- ✅ 节点卡片显示选中的知识库名称
- ✅ 节点卡片显示选中的 Embedding 模型名称（如果选择了）
- ✅ 节点卡片显示检索参数（Top K, 阈值）

**预览行格式：**
```
知识库: 知识库A, 知识库B
向量模型: text-embedding-3-small (如果选择了)
Top K: 5
阈值: 0.5
```

---

### 测试 5：完整流程 ✅

**步骤：**
1. 创建新工作流
2. 添加开始节点（定义输入参数 `query`）
3. 添加 RAG 检索节点
4. 配置知识库、模型、输出变量
5. 添加 LLM 节点引用 RAG 输出（如 `${rag_1.documents}`）
6. 保存工作流
7. 刷新页面
8. 重新打开工作流

**预期结果：**
- ✅ 配置正确保存到数据库
- ✅ 重新打开后配置保持不变
- ✅ 节点预览显示正确信息
- ✅ 输出变量引用正常工作

---

## 改进对比

### 改进前
- 知识库选择：硬编码假数据 `kb-1`, `kb-2`
- 模型选择：无配置选项
- 输出变量：手动输入提取路径

### 改进后
- ✅ 知识库选择：从系统获取真实列表，显示文档数量
- ✅ 模型选择：从系统获取 Embedding 模型，显示默认标识
- ✅ 输出变量：预设 5 个常用输出，快速选择按钮

---

## 技术实现

### 新增功能
1. **知识库 Store 集成**
   - 导入 `useKnowledgeStore`
   - 使用 `kbList` 属性获取知识库列表
   - 使用 `loadKbList()` 方法加载数据

2. **Embedding 模型选择**
   - 从 `useSettingsStore().models.embed` 获取模型列表
   - 支持可选配置（clearable）
   - 显示默认模型标识

3. **输出变量预设**
   - 定义 `ragOutputPresets` 数组
   - 使用 `isRAGOutputDef` 计算属性判断节点类型
   - 提供快速选择按钮和下拉选择

### 文件修改
- `frontend/src/views/workflow/components/NodeConfigModal.vue`
  - 新增：知识库 Store 导入和初始化
  - 新增：计算属性 `enabledKnowledgeBases`, `enabledEmbedModels`, `ragOutputPresets`, `isRAGOutputDef`
  - 修改：知识库选择 UI（从硬编码改为系统数据）
  - 新增：Embedding 模型选择 UI
  - 修改：输出变量定义 UI（支持 RAG 预设）
  - 修改：节点预览行（显示知识库和模型）

---

## 测试结论

所有改进已完成，建议进行以下手动测试：
1. 在真实环境中测试知识库选择功能
2. 测试不同 Embedding 模型的选择和切换
3. 验证输出变量预设的实用性
4. 检查节点预览是否正确显示配置信息

**改进完成日期：** 2026-09-16
**改进状态：** ✅ 完成