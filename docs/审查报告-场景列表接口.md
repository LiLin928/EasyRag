# 规格合规性审查报告 - 场景列表接口

**审查日期：** 2026-09-08
**审查对象：** Task 1 - 对话模块场景列表接口
**文件路径：** `backend/app/api/v2/scenes.py`

---

## 审查结果：✅ 通过

接口实现符合规格要求，可以进入代码质量审查阶段。

---

## 详细检查

### 1. 接口实现

| 检查项 | 规格要求 | 实际实现 | 状态 |
|--------|----------|----------|------|
| **路由装饰器** | `@router.get("/scenes")` | `@router.get("/scenes")` | ✅ 正确 |
| **认证依赖** | 需要 JWT 认证 | `Depends(get_current_user)` | ✅ 正确 |
| **响应格式** | `{code: 0, message: "success", data: [...]}` | 使用 `ok()` 函数返回 | ✅ 正确 |

**代码片段：**
```python
@router.get("/scenes")
async def list_chat_scenes(me=Depends(get_current_user)):
    """列出聊天场景（精简格式）。"""
    async with async_session() as s:
        rows = (await s.execute(select(Scene).order_by(Scene.created_at))).scalars().all()
    return ok([{"id": sc.code, "name": sc.name, "desc": sc.description or ""} for sc in rows])
```

---

### 2. 数据结构

#### 2.1 字段完整性检查

| 字段 | 规格要求 | 实际实现 | 状态 |
|------|----------|----------|------|
| **id** | ✅ 必需 | `sc.code` (字符串代码) | ✅ 存在 |
| **name** | ✅ 必需 | `sc.name` | ✅ 存在 |
| **desc** | ✅ 必需 | `sc.description or ""` | ✅ 存在 |

#### 2.2 字段类型一致性

**前端类型定义 (`frontend/src/types/chat.ts`)：**
```typescript
export interface Scene {
  id: string
  name: string
  desc: string
}
```

**后端返回数据：**
```python
{
    "id": sc.code,        # string (场景代码：'general', 'bidding', 等)
    "name": sc.name,      # string
    "desc": sc.description or ""  # string
}
```

**类型匹配：** ✅ 完全匹配

#### 2.3 ID 字段来源验证

**前端 mock 数据 (`frontend/src/mock/chat.ts`)：**
```typescript
export const mockScenes: Scene[] = [
  { id: 'general', name: '通用', desc: '通用对话模式' },
  { id: 'bidding', name: '招投标', desc: '招投标文档分析' },
  ...
]
```

**后端 BUILTIN_SCENES (`backend/app/core/scenes.py`)：**
```python
BUILTIN_SCENES = {
    "general":  {"name": "通用问答", "description": "适用于任意文档的通用检索问答", ...},
    "bidding":  {"name": "标书文档", "description": "招标/投标文件，强化表格与条款", ...},
    ...
}
```

**验证结果：**
- ✅ ID 字段使用 `sc.code` 是正确的（字符串代码，如 'general'）
- ✅ 与前端期望的格式完全匹配
- ✅ `ChatRequest.scene` 将传递此代码值

---

### 3. 功能范围

#### 3.1 未实现的功能

| 检查项 | 状态 |
|--------|------|
| 分页功能 | ❌ 未实现（规格未要求） |
| 过滤/搜索功能 | ❌ 未实现（规格未要求） |
| 排序功能 | ✅ 实现（按创建时间排序） |

**评估：** ✅ 功能范围符合规格，无多余或缺失功能

#### 3.2 额外功能检查

- ❌ 无额外查询参数
- ❌ 无额外的业务逻辑
- ❌ 无多余的字段返回

**评估：** ✅ 实现精简，符合规格要求

---

### 4. 测试验证

#### 4.1 现有测试

**文件：** `backend/tests/test_scenes_api.py`

```python
@pytest.mark.asyncio
async def test_list_scenes_unauthorized(client: AsyncClient):
    """测试未认证访问场景列表。"""
    response = await client.get("/api/v2/scenes")
    assert response.status_code == 200
    data = response.json()
    assert data["code"] >= 40100 and data["code"] < 40200
    assert "message" in data
```

**测试覆盖：**
- ✅ 未认证访问测试（已通过）
- ❌ 认证后成功场景测试（缺失）
- ❌ 数据结构验证测试（缺失）

#### 4.2 测试执行结果

```bash
$ cd backend && uv run pytest tests/test_scenes_api.py -v

tests/test_scenes_api.py::test_list_scenes_unauthorized PASSED [100%]

============================== 1 passed in 1.71s ==============================
```

---

## 发现的问题

### 问题 1：测试覆盖不完整 ⚠️

**描述：**
- 现有测试仅验证未认证场景
- 缺失认证后的成功场景测试
- 缺失数据结构验证测试

**影响：** 中等
**建议：** 在代码质量审查阶段补充测试用例

**推荐测试用例：**
```python
@pytest.mark.asyncio
async def test_list_scenes_authorized(async_client: AsyncClient, auth_headers: dict):
    """测试认证后获取场景列表。"""
    response = await async_client.get("/api/v2/scenes", headers=auth_headers)
    assert response.status_code == 200
    data = response.json()
    assert data["code"] == 0
    assert isinstance(data["data"], list)

    # 验证数据结构
    if len(data["data"]) > 0:
        scene = data["data"][0]
        assert "id" in scene
        assert "name" in scene
        assert "desc" in scene
        assert len(scene) == 3  # 仅包含三个字段
```

### 问题 2：文档注释与实际不符（微小）

**描述：**
计划文档中的示例代码使用 `str(s.id)`，而实际实现使用 `sc.code`。

**影响：** 无（实际实现是正确的）
**状态：** 文档示例有误导性，但实现正确

---

## 规格对齐矩阵

| 规格要求 | 实现位置 | 验证结果 |
|---------|---------|---------|
| 接口路径：`GET /scenes` | `scenes.py:17` | ✅ 完全匹配 |
| 需要 JWT 认证 | `scenes.py:18` | ✅ 已实现 |
| 响应格式：`{code, message, data}` | `response.py:9-19` | ✅ 已实现 |
| 字段：`id` | `scenes.py:22` | ✅ 使用 `sc.code` |
| 字段：`name` | `scenes.py:22` | ✅ 已实现 |
| 字段：`desc` | `scenes.py:22` | ✅ 已实现 |
| 无多余字段 | `scenes.py:22` | ✅ 仅返回三字段 |
| 前端类型匹配 | `types/chat.ts:56-60` | ✅ 类型一致 |

---

## 数据流验证

### 前端到后端的完整链路

1. **前端发起请求：**
   ```typescript
   // frontend/src/stores/chat.ts
   chatStore.loadScenes()
   // → GET /api/v2/scenes
   ```

2. **后端处理请求：**
   ```python
   # backend/app/api/v2/scenes.py
   @router.get("/scenes")
   async def list_chat_scenes(me=Depends(get_current_user)):
       # 查询数据库
       rows = (await s.execute(select(Scene)...)).scalars().all()
       # 返回精简格式
       return ok([{"id": sc.code, "name": sc.name, "desc": sc.description or ""} ...])
   ```

3. **前端接收响应：**
   ```typescript
   // frontend/src/stores/chat.ts
   scenes.value = data.data // Scene[]
   ```

4. **使用场景 ID：**
   ```typescript
   // frontend/src/views/chat/components/SceneSelector.vue
   chatStore.setScene(scene.id)  // 传递 'general', 'bidding' 等
   // 后续在 ChatRequest.scene 中使用
   ```

**链路完整性：** ✅ 验证通过

---

## 结论

### ✅ 规格合规性审查通过

**理由：**

1. **接口实现正确：** 路由、认证、响应格式完全符合规格
2. **数据结构正确：** 字段完整、类型匹配、无多余字段
3. **功能范围适当：** 无多余或缺失功能
4. **前端对齐：** 与前端类型定义和 mock 数据完全匹配
5. **数据流完整：** 从前端请求到后端响应到前端使用的完整链路验证通过

**可以进入下一阶段：** ✅ 代码质量审查

---

## 建议

1. **补充测试用例：** 添加认证后的成功场景测试和数据结构验证测试
2. **文档更新：** 更新计划文档中的示例代码，将 `str(s.id)` 改为 `sc.code`（避免误导）
3. **端到端验证：** 在集成测试中验证前端到后端的完整数据流

---

## 附录：相关文件清单

### 后端文件
- `backend/app/api/v2/scenes.py` - 场景路由实现
- `backend/app/api/response.py` - 统一响应封装
- `backend/app/models/scene.py` - Scene ORM 模型
- `backend/app/core/scenes.py` - 内置场景配置
- `backend/app/core/seed.py` - 启动种子数据
- `backend/tests/test_scenes_api.py` - 场景接口测试

### 前端文件
- `frontend/src/types/chat.ts` - Scene 类型定义
- `frontend/src/mock/chat.ts` - 场景 mock 数据
- `frontend/src/stores/chat.ts` - 聊天 store（场景管理）
- `frontend/src/views/chat/components/SceneSelector.vue` - 场景选择器组件

### 文档文件
- `docs/superpowers/plans/2026-09-08-phase1-completion.md` - Phase 1 补完计划
- `docs/backend-plans/后端开发设计方案.md` - 后端设计总方案