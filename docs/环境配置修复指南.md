# 环境配置修复指南

## 修复概述

**修复时间**: 2026-09-09
**状态**: ✅ 已完成配置文件修复，需执行数据库脚本

---

## 已完成的修复

### ✅ 1. 更新 .env 配置文件

**修改内容**:
- ❌ 删除废弃的 `REDIS_URL` 配置
- ✅ 添加 MinIO 存储配置
- ✅ 添加 Tracing 配置（默认 none）
- ✅ 添加模型配置注释说明

**文件位置**: `backend/.env` 和 `backend/.env.example`

### ✅ 2. 创建数据库初始化脚本

**新增文件**: `deploy/init-databases.sql`

**功能**:
- 创建 `easyrag` 用户
- 创建 `easyrag` 数据库（V1，保留）
- 创建 `easyrag_v2` 数据库（V2，当前使用）
- 创建 `langfuse` 用户和数据库
- 启用必要扩展（vector, pg_trgm, uuid-ossp）
- 设置权限

---

## 需要执行的修复步骤

### 🔴 高优先级：执行数据库初始化

**步骤 1**: 连接到 PostgreSQL

```bash
# 在虚拟机上执行
psql -U postgres -h 192.168.137.13
```

**步骤 2**: 执行初始化脚本

方法 A（直接在虚拟机上）:
```bash
# 在虚拟机上执行
psql -U postgres -h localhost -f /path/to/init-databases.sql
```

方法 B（从本机执行）:
```bash
# 在本机执行
cd D:\4-MyProject\EasyRag
psql -U postgres -h 192.168.137.13 -f deploy/init-databases.sql
```

方法 C（使用 Docker）:
```bash
# 在虚拟机上执行
docker exec -i easyrag-postgres psql -U postgres < deploy/init-databases.sql
```

**步骤 3**: 验证数据库创建

```bash
# 连接到 PostgreSQL
psql -U postgres -h 192.168.137.13

# 查看数据库列表
\l

# 应该看到:
# - easyrag (V1)
# - easyrag_v2 (V2)
# - langfuse

# 查看用户列表
\du

# 应该看到:
# - easyrag
# - langfuse
```

**步骤 4**: 验证扩展

```bash
# 连接到 easyrag_v2
\c easyrag_v2

# 查看已安装扩展
\dx

# 应该看到:
# - vector
# - pg_trgm
# - uuid-ossp
```

---

## 验证修复结果

### 1. 验证后端配置

```bash
cd backend

# 测试配置加载
uv run python -c "
from app.config import settings
print('Database URL:', settings.database_url)
print('MinIO Endpoint:', settings.minio_endpoint)
print('Storage Type:', settings.storage_type)
print('Tracing Provider:', settings.tracing_provider)
"
```

**预期输出**:
```
Database URL: postgresql+asyncpg://easyrag:easyrag2026@192.168.137.13:5432/easyrag_v2
MinIO Endpoint: 192.168.137.13:9000
Storage Type: minio
Tracing Provider: none
```

### 2. 验证数据库连接

```bash
cd backend

# 测试数据库连接
uv run python -c "
import asyncio
from app.db.session import async_session

async def test():
    async with async_session() as s:
        result = await s.execute('SELECT 1')
        print('Database connection successful!')

asyncio.run(test())
"
```

### 3. 验证 MinIO 连接

```bash
# 测试 MinIO 健康
curl http://192.168.137.13:9000/minio/health/live

# 应该返回: OK
```

### 4. 验证 Langfuse

```bash
# 测试 Langfuse 健康
curl http://192.168.137.13:3030/api/health

# 应该返回健康状态
```

### 5. 运行后端测试

```bash
cd backend

# 运行基础测试
uv run pytest tests/test_config.py tests/test_health.py -v
```

---

## 可选配置（根据需要）

### 1. 配置 LLM 模型

如果需要默认模型配置，编辑 `.env`:

```bash
# 默认模型配置
LLM_DEFAULT_BASE_URL=https://api.openai.com/v1
LLM_DEFAULT_API_KEY=your-api-key
LLM_QA_MODEL=gpt-4
LLM_FAST_MODEL=gpt-3.5-turbo
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIM=1024
RERANK_MODEL=your-rerank-model
```

### 2. 启用 Tracing

如果需要 LLM 调用追踪，编辑 `.env`:

```bash
# 使用 Langfuse（推荐）
TRACING_PROVIDER=langfuse
LANGFUSE_PUBLIC_KEY=pk-xxx
LANGFUSE_SECRET_KEY=sk-xxx
LANGFUSE_HOST=http://192.168.137.13:3030

# 或使用 LangSmith
# TRACING_PROVIDER=langsmith
# LANGSMITH_API_KEY=your-api-key
# LANGSMITH_PROJECT=easyrag
```

---

## 故障排除

### 问题 1: 数据库连接失败

**症状**: `Connection refused` 或 `password authentication failed`

**解决方案**:
1. 检查 PostgreSQL 是否运行: `docker ps | grep postgres`
2. 检查用户是否存在: `psql -U postgres -c "\du"`
3. 检查密码是否正确

### 问题 2: MinIO 连接失败

**症状**: `Connection refused` 或 `access denied`

**解决方案**:
1. 检查 MinIO 是否运行: `docker ps | grep minio`
2. 检查访问密钥: `docker exec easyrag-minio mc alias list`

### 问题 3: Langfuse 404 错误

**症状**: Langfuse UI 无法访问

**解决方案**:
1. 检查 Langfuse 服务: `docker ps | grep langfuse`
2. 检查数据库: `psql -U langfuse -d langfuse -c "SELECT 1"`

---

## 下一步

修复完成后：

1. ✅ 运行完整的测试套件
2. ✅ 验证所有 API 接口
3. ✅ 准备前后端集成测试
4. ✅ 考虑生产环境部署

---

**修复完成时间**: 2026-09-09
**执行状态**: ⏳ 需要执行数据库初始化脚本
**验证状态**: ⏳ 等待用户验证